{% extends 'base.html.twig' %}

{% block title %}

    {{  series is defined ? 'Editar' : 'Nova'}} Série

{% endblock %}

{% block body %}

    <a href="{{ path('app_series') }}" class="btn btn-dark mb-3">Voltar</a>

    {{ form_start(seriesForm) }}
    {{ form_row(seriesForm.seriesName) }}
    {{ form_row(seriesForm.seasonsQuantity) }}
    {{ form_row(seriesForm.episodesPerSeason) }}
    {# {{ form_widget(seriesForm.name, {'attr': {'class': 'form-control-sm'}}) }} #}
    {{ form_widget(seriesForm.save, {'attr': {'class': 'btn-dark'}}) }}
    {{ form_end(seriesForm) }}

{% endblock %}
