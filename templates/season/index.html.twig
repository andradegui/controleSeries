{% extends 'base.html.twig' %}

{% block title %}Temporadas de {{ series.name }}{% endblock %}

{% block body %}

    <div class="container-fluid d-flex justify-content-between align-items-center p-2">
        <a href="{{ path('app_series') }}" class="btn btn-dark">Home</a>
        <a href="{{ path('app_logout') }}" class="btn btn-danger">Sair</a>
    </div>

    <ul class="list-group m-3">

    {% for season in seasons %}

    <li class="list-group-item d-flex justify-content-between align-items-center">

        <a href="{{ path('app_episodes', {season: season.id })}}" class="btn btn-light active">
            Temporada {{ season.number }}
        </a>

        <div class="d-flex">
            <span class="badge badge-pill bg-secondary">
                {{ season.watchedEpisodes|length }} / {{ season.episodes|length }}
            </span>
        </div>

    </li>

    {% endfor %}
    
    </ul>

{% endblock %}
